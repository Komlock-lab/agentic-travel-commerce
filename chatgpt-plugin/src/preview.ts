export const previewHtml = String.raw`<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ChatGPT UI Preview — Agentic Travel Commerce</title>
  <style>
    :root{--text:#202123;--muted:#6b6f76;--line:#e5e5e5;--soft:#f7f7f8;--sidebar:#f9f9f9;--accent:#3157e8}*{box-sizing:border-box}html,body{margin:0;height:100%;color:var(--text);font:14px/1.5 -apple-system,BlinkMacSystemFont,"Segoe UI","Hiragino Sans","Yu Gothic",sans-serif}body{display:grid;grid-template-columns:248px 1fr;background:white}.sidebar{padding:12px;display:flex;flex-direction:column;gap:16px;background:var(--sidebar);border-right:1px solid var(--line)}.new-chat,.history{padding:10px 12px;border-radius:9px}.new-chat{display:flex;align-items:center;gap:10px;font-weight:650}.new-chat:hover,.history.active{background:#ececec}.history{color:#555}.side-title{padding:8px 12px 0;color:#8a8a8a;font-size:11px}.side-bottom{margin-top:auto;padding:12px;color:#777;border-top:1px solid var(--line)}.chat{min-width:0;display:grid;grid-template-rows:52px 1fr}.chat-head{padding:0 18px;display:flex;align-items:center;gap:10px;border-bottom:1px solid var(--line);font-weight:650}.preview-badge{padding:3px 7px;border-radius:999px;color:#666;background:#f1f1f1;font-size:10px}.viewport{overflow:auto}.thread{width:min(100%,780px);min-height:100%;margin:auto;padding:34px 24px 150px}.demo-bar{margin-bottom:28px;padding:10px 12px;display:flex;align-items:center;justify-content:space-between;gap:12px;border:1px solid var(--line);border-radius:12px;background:#fafafa}.demo-bar span{font-size:12px;color:var(--muted)}.modes{display:flex;gap:6px}.mode{padding:6px 9px;border:1px solid var(--line);border-radius:8px;background:white;font:inherit;font-size:11px;cursor:pointer}.mode.active{color:white;background:#202123;border-color:#202123}.message{margin:0 0 24px}.user{max-width:76%;margin-left:auto;padding:10px 14px;border-radius:18px;background:#f3f3f3}.assistant{display:grid;grid-template-columns:28px 1fr;gap:11px;align-items:start}.avatar{width:28px;height:28px;display:grid;place-items:center;border:1px solid var(--line);border-radius:50%;font-weight:800;font-size:11px}.assistant-copy{padding-top:3px}.app-block{margin:4px 0 26px 39px}.app-label{margin-bottom:7px;display:flex;align-items:center;gap:7px;color:#666;font-size:11px;font-weight:650}.app-icon{width:18px;height:18px;display:grid;place-items:center;border-radius:5px;color:white;background:#202123;font-size:9px}.app-frame{display:block;width:100%;height:180px;border:1px solid var(--line);border-radius:14px;background:white;overflow:hidden}.composer-wrap{position:fixed;left:248px;right:0;bottom:0;padding:18px 24px 24px;pointer-events:none;background:linear-gradient(transparent,white 32%)}.composer,.hint{pointer-events:auto}.composer{width:min(100%,760px);margin:auto;padding:11px 12px 11px 16px;display:flex;align-items:end;gap:10px;border:1px solid #d5d5d5;border-radius:24px;background:white;box-shadow:0 2px 12px rgba(0,0,0,.06)}.composer textarea{min-height:24px;max-height:100px;flex:1;resize:none;border:0;outline:0;background:transparent;font:inherit}.send{width:32px;height:32px;border:0;border-radius:50%;color:white;background:#202123;cursor:pointer}.hint{width:min(100%,760px);margin:6px auto 0;text-align:center;color:#999;font-size:10px}
    @media(max-width:760px){body{display:block}.sidebar{display:none}.chat{height:100%}.thread{padding:24px 14px 140px}.composer-wrap{left:0;padding:14px}.app-block{margin-left:0}.demo-bar{align-items:flex-start;flex-direction:column}}
  </style>
</head>
<body>
  <aside class="sidebar">
    <div class="new-chat"><span>✦</span> ChatGPT</div>
    <div><div class="side-title">今日</div><div class="history active">京都旅行の手配</div><div class="history">Agentic Commerceの相談</div></div>
    <div class="side-bottom">Komlock Lab demo</div>
  </aside>
  <main class="chat">
    <header class="chat-head">Agentic Travel Commerce <span class="preview-badge">ChatGPT UI preview</span></header>
    <div class="viewport" id="viewport"><section class="thread" id="thread">
      <div class="demo-bar"><span>デモ結果を切り替える</span><div class="modes"><button class="mode active" data-scenario="happy">通常</button><button class="mode" data-scenario="price_change">価格変更</button><button class="mode" data-scenario="denied">拒否</button></div></div>
      <div id="messages"></div>
    </section></div>
    <div class="composer-wrap"><form class="composer" id="composer"><textarea id="prompt" rows="1" placeholder="「夕食なし」「川沿いの宿に変更」など"></textarea><button class="send" aria-label="送信">↑</button></form><div class="hint">これはChatGPT内での表示を再現したローカルモックです</div></div>
  </main>
  <script>
    const messages=document.getElementById('messages');const viewport=document.getElementById('viewport');const frames=new Map();let scenario='happy';let state=null;let activeFrame=null;
    const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
    const scrollDown=()=>requestAnimationFrame(()=>viewport.scrollTo({top:viewport.scrollHeight,behavior:'smooth'}));
    function addUser(text){messages.insertAdjacentHTML('beforeend','<div class="message user">'+esc(text)+'</div>');scrollDown()}
    function addAssistant(text){messages.insertAdjacentHTML('beforeend','<div class="message assistant"><span class="avatar">✦</span><div class="assistant-copy">'+text+'</div></div>');scrollDown()}
    function addWidget(next){state=next;const iframe=document.createElement('iframe');iframe.className='app-frame';iframe.src='/widget';iframe.title='Komlock Lab interactive card';const block=document.createElement('div');block.className='app-block';block.innerHTML='<div class="app-label"><span class="app-icon">K</span>Komlock Lab</div>';block.appendChild(iframe);messages.appendChild(block);activeFrame=iframe;frames.set(iframe.contentWindow,iframe);iframe.addEventListener('load',()=>sendState(iframe,next));scrollDown()}
    function sendState(frame=activeFrame,next=state){frame?.contentWindow?.postMessage({jsonrpc:'2.0',method:'ui/notifications/tool-result',params:{structuredContent:next}},'*')}
    async function postJson(url,body){const response=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const json=await response.json();if(!response.ok)throw new Error(json.error||'Request failed');return json}
    async function reset(nextScenario){scenario=nextScenario;document.querySelectorAll('.mode').forEach(button=>button.classList.toggle('active',button.dataset.scenario===scenario));messages.innerHTML='';addUser('10月12日から14日、2名で京都へ。予算12万円。駅に近い宿と夕食、体験を入れたいです。2万円以下で変更可能なら自動決済してOK。');addAssistant('承知しました。まず、AIに任せる支払い範囲を確認してください。');state=await postJson('/preview/reset',{scenario});addWidget(state)}
    function followup(next){if(next.view==='inventory')return '条件に合う候補をカテゴリ別に見つけました。おすすめを選択していますが、<strong>宿・体験・夕食は自由に組み替えられます。</strong>';if(next.view==='approval')return 'この組み合わせで予約可能です。上限を超えるものと取消不可の商品だけ、理由を確認してください。';if(next.view==='exception')return 'ホテル価格の上昇を検知したため、支払い前で止めました。代替へ変更するか、新価格を承認できます。';if(next.status==='denied')return '支払い先が許可された旅行在庫と一致しなかったため、資金移動前に拒否しました。';return '選んだ内容で予約が完了しました。実課金ではなくカードSandboxでの処理です。'}
    async function handleTool(source,id,params){try{const next=await postJson('/preview/action',params);state=next;source.postMessage({jsonrpc:'2.0',id,result:{structuredContent:next,content:[]}},'*');setTimeout(()=>addAssistant(followup(next)),120)}catch(error){source.postMessage({jsonrpc:'2.0',id,error:{code:-32000,message:error.message}},'*')}}
    window.addEventListener('message',event=>{const message=event.data;if(!message||message.jsonrpc!=='2.0')return;if(message.method==='ui/initialize'){event.source.postMessage({jsonrpc:'2.0',id:message.id,result:{protocolVersion:'2026-01-26',hostInfo:{name:'chatgpt-preview',version:'0.2.0'},hostCapabilities:{}}},'*');return}if(message.method==='tools/call'){handleTool(event.source,message.id,message.params);return}if(message.method==='ui/notifications/size-changed'){const frame=frames.get(event.source);if(frame)frame.style.height=Math.max(100,Math.ceil(message.params?.height||180))+'px'}});
    document.querySelectorAll('.mode').forEach(button=>button.addEventListener('click',()=>reset(button.dataset.scenario)));
    document.getElementById('composer').addEventListener('submit',event=>{event.preventDefault();const input=document.getElementById('prompt');const text=input.value.trim();if(!text)return;input.value='';addUser(text);if(!state||state.view!=='inventory'){addAssistant('了解しました。このモックでは候補選択中に、宿・体験・夕食の希望を会話で変更できます。');return}let changed=false;if(/夕食.*(なし|不要)|食事.*(なし|不要)/.test(text)){state.selectedItemIds=state.selectedItemIds.filter(id=>!state.inventory.some(item=>item.id===id&&item.category==='food'));changed=true}if(/体験.*(なし|不要)/.test(text)){state.selectedItemIds=state.selectedItemIds.filter(id=>!state.inventory.some(item=>item.id===id&&item.category==='activity'));changed=true}if(/川沿い|鴨川/.test(text)){state.selectedItemIds=state.selectedItemIds.filter(id=>!state.inventory.some(item=>item.id===id&&item.category==='lodging'));state.selectedItemIds.push('hotel_kamo');changed=true}if(/駅近|駅に近/.test(text)){state.selectedItemIds=state.selectedItemIds.filter(id=>!state.inventory.some(item=>item.id===id&&item.category==='lodging'));state.selectedItemIds.push('hotel_sora');changed=true}sendState();addAssistant(changed?'希望に合わせて選択を更新しました。カード上でもさらに変更できます。':'希望を反映するには「夕食なし」「体験なし」「川沿いの宿」のように入力してください。')});
    reset('happy');
  </script>
</body>
</html>`;
